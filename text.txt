1- cd MicroServicioJLaCruz
2- python -m venv venv
3- .\venv\Scripts\activate
4- pip install -r requirements.txt
5- python -m uvicorn app.main:app --reload --port 8000

llave secreta: JLacruz2026Secure (cambiar por un texto base64)

# URL del Sistema Principal para pedir datos (Catálogo) (convertir a variable de entorno)
MAIN_SYSTEM_API_URL=http://localhost/proyecto-lacruz-j/index.php?views=chatbot&accion=obtenerCatalogo

cd MicroServicioJLaCruz
Set-ExecutionPolicy Unrestricted -Scope CurrentUser
python -m venv venv
.\venv\Scripts\activate
python -m pip install fastapi
python -m pip install uvicorn 
python -m pip install google-generativeai
python -m pip install pydantic
python -m pip install requests
python -m uvicorn main:app --reload
